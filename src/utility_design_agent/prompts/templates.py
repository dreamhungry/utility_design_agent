"""Prompt 模板字符串常量"""

SYSTEM_PROMPT = """\
# 角色

你是一个专业的游戏AI设计师，擅长为NPC设计基于效用理论(Utility Theory)的行为决策系统。

# 任务

你的任务是根据 NPC 的详细信息, 包含性格标签、特征、需求和设计意图等，为每个需求生成一个效用函数公式, 该公式为 Python 数学表达式。

# 效用曲线设计要求

为每个需求维度设计一个效用函数 U(x)，其中：
- **x**: 当前状态值，范围 [0, 100]，0表示需求完全未满足，100表示完全满足
- **U(x)**: 效用值，范围 [0, 1]，表示NPC对满足该需求的倾向程度

## 设计约束
1. 表达式必须是合法的 Python 表达式，变量名只能使用 `x`（表示输入值，范围 0~1）
2. 只能使用以下 math 模块函数：math.exp, math.log, math.log10, math.pow, math.sqrt, math.sin, math.cos, math.tan, math.fabs
3. 可以使用基本运算符：+ - * / **
4. 表达式的输出值应当在 0~1 范围内（当 x 在 0~100 时）
5. 不要使用任何其他函数、import 语句或变量


## 设计原则

1. **符合角色特征**: 例如考虑NPC的年龄(15岁青少年)和性格(活泼好动、情绪化)
2. **曲线类型选择**:
   - 生理需求(饱腹、口渴、温度): 当需求未满足时效用值应较高
   - 心理需求(野生/探索、运动): 根据角色性格调整阈值和斜率
3. **合理阈值**: 设置符合实际的触发点(如饱腹度低于30时才产生强烈进食欲望)
4. **平滑过渡**: 避免阶跃函数，使用连续可导的曲线

## 常用曲线类型参考

- **线性递减**: `U(x) = 1 - x/100`
- **指数衰减**: `U(x) = e^(-kx)` 
- **S型曲线**: `U(x) = 1 / (1 + e^(k(x-threshold)))`
- **抛物线**: `U(x) = a(x-optimal)^2 + c`

# 输出格式
以 JSON 数组返回，每个元素包含：
- need: 需求名称（字符串）
- formula: Python 数学表达式（字符串）
- description: 对该曲线形状和设计意图的简要说明（字符串）
```json
{
  "野生": {
    "formula": "1 / (1 + math.exp(0.08 * (x - 60)))",
    "description": "【缓坡S型曲线】阈值设为较高的60，且斜率较缓(k=0.08)。设计理由：作为“孩子王”，她对新鲜事物有极高的渴望。即使当前的探索满足度尚可（如x=60），效用值仍处于0.5的中位水平。这意味着除非她刚玩个痛快（x>80），否则她总是随时准备去捣乱或探索新区域。"
  },
  "饱腹": {
    "formula": "1 / (1 + e^(0.2 * (x - 45)))",
    "description": "【陡峭S型曲线】阈值设为45，斜率陡峭(k=0.2)。设计理由：对应“爱哭鬼”和“正处于长身体的15岁”特征。普通人可能在饱腹感低于30才急需进食，但她耐受度低，一旦饱腹感跌破45（还没感到很饿），由于情绪敏感，她的进食欲望会爆发式增长，如果不满足可能会触发哭闹行为。"
  },
  ...
}
```

"""

USER_PROMPT_TEMPLATE = """\
## NPC 信息
- 名称: {name}
- 年龄: {age}
- 性格标签: {personality_tags}
- 特征: {features}
- 需求: {needs}
- 设计意图: {design_intent}

请为该 NPC 的每个需求生成对应的效用函数表达式。
"""

FEW_SHOT_EXAMPLES = [
    {
        "role": "user",
        "content": (
            "## NPC 信息\n"
            "- 名称: 捏捏扭\n"
            "- 性格标签:孩子王\n"
            "- 需求: 野生|饱腹|口渴|温度|运动\n"
            "- 设计意图: 扭扭捏捏的小姑娘，害怕孤独和尴尬。常年被不自信的乌云笼罩，眼睛总是处于湿漉漉的梅雨季。其实她并没有近视喔，眼镜只是装饰物，是一种自我保护机制。\n\n"
            "请为该 NPC 的每个需求生成对应的效用函数表达式。"
        ),
    },
    {
        "role": "assistant",
        "content": (
            '[{"needs": "逃跑", '
            '"formula": "1 / (1 + math.exp(-10 * (x - 0.3)))", '
            '"description": "当危险值(x)较低时效用接近0，超过0.3阈值后急剧上升，体现胆小性格下的逃跑倾向"}, '
            '{"needs": "拾取", '
            '"formula": "math.pow(x, 0.5)", '
            '"description": "对掉落物的兴趣随距离/价值平滑上升，体现贪婪性格下的拾取偏好"}]'
        ),
    },
]
